PORT=4000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/hunger_cafe
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=8h
# Comma-separated allowlist. In development, any http://localhost:* origin is also allowed (Flutter web uses random ports).
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
